import { XmlresponseInterceptor } from './xmlresponse.interceptor';

describe('XmlresponseInterceptor', () => {
  it('should be defined', () => {
    expect(new XmlresponseInterceptor()).toBeDefined();
  });
});
